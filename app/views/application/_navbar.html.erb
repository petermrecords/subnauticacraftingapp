<nav class="navbar fixed-top navbar-expand-md navbar-light bg-light">
  <span class="navbar-brand">CRAFTING APP</span>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbar-nav">
    <span class="navbar-toggler-icon"></span>
  </button>
  <% if user_signed_in? %>
    <div class="collapse navbar-collapse" id="navbar-nav">
      <ul class="navbar-nav">
        <li class="nav-item">
          <%= link_to "Make A List", new_user_list_path(@user), class: "nav-link" %>
        </li>
        <li class="nav-item">
          <%= link_to "My Lists", user_lists_path(@user), class: "nav-link" %>
        </li>
        <li class="nav-item dropdown">
          <a href="#" class="nav-link dropdown-toggle" id="navbar-dropdown" data-toggle="dropdown"><%= @user.email %></a>
          <div class="dropdown-menu dropdown-menu-right">
            <%= link_to "Logout", destroy_user_session_path, method: :delete, class: "dropdown-item" %>
            <%= link_to "Edit Account", edit_user_registration_path(@user), class: "dropdown-item" %>
            <div class="dropdown-divider"></div>
            <%= link_to "Delete Account", cancel_user_registration_path(@user), class: "dropdown-item text-danger" %>
          </div>
        </li>
      </ul>
    </div>
  <% end %>
</nav>